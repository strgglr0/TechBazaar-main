# Development environment configuration for TechBazaar

# Frontend development server port
VITE_DEV_SERVER_PORT=3000

# Flask backend port
FLASK_RUN_PORT=5001

# API proxy target (where frontend should proxy API calls)
VITE_API_PROXY_TARGET=http://localhost:5001

# HMR configuration (automatically set for Codespaces)
# HMR_PROTOCOL=wss
# HMR_HOST=

# Flask secret key for development
FLASK_SECRET=dev-secret-key-change-in-production

# Database URL (for development using SQLite)
DATABASE_URL=sqlite:///data.db